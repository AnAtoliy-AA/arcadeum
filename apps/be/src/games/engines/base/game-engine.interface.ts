/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export type {
  GameMetadata,
  GamePlayerState,
  GameLogEntry,
  BaseGameState,
  GameResult,
  GameActionResult,
  GameActionContext,
  IGameEngine,
  ChatScope,
} from '@arcadeum/games-core/base/game-engine.interface';
