/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  buildDeck,
  shuffle,
  isActiveColor,
  isPlayable,
  pickStarterIndex,
  nextIndex,
} from '@arcadeum/games-core/games/cascade/cascade.utils';
