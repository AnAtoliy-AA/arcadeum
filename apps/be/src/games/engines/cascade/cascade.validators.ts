/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  validatePlayCard,
  validateDraw,
  validateNameColor,
  validateForfeit,
} from '@arcadeum/games-core/games/cascade/cascade.validators';
