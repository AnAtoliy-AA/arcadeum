/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  MIN_PLAYERS,
  MAX_PLAYERS,
  TRACK_LENGTH,
  POWER_TOKENS_PER_GAME,
  TRACK_TYPES,
  THEMES,
  CAT_IDS,
  CAT_ABILITIES,
  THEME_BONUSES,
  DEFAULT_OPTIONS,
} from '@arcadeum/games-core/games/cat-dash/cat-dash.constants';
export type {
  TrackType,
  Theme,
  CatId,
} from '@arcadeum/games-core/games/cat-dash/cat-dash.constants';
