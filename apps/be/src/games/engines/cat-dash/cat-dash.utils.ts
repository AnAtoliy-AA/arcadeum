/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  generateTrack,
  calculateMovement,
  checkWinCondition,
  getAvailableAbilities,
  applySpaceEffect,
  hasThemeBonus,
  rollDice,
  sanitizeCatDashState,
  getAvailableActions,
} from '@arcadeum/games-core/games/cat-dash/cat-dash.utils';
