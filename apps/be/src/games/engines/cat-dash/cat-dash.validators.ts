/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  validateRollDice,
  validateUseAbility,
  validateChoosePath,
  validateForfeit,
} from '@arcadeum/games-core/games/cat-dash/cat-dash.validators';
