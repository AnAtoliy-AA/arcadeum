/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  ctx,
  s,
  singleStep,
  capture,
  multiCapture,
} from '@arcadeum/games-core/games/checkers/checkers-test-utils';
