/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  applyBotMove,
  hasNonPawnMaterial,
  hashState,
  scoreMove,
} from '@arcadeum/games-core/games/chess/chess-bot-utils';
