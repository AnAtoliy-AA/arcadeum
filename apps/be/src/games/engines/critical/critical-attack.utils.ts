/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  executeTargetedAttack,
  executePersonalAttack,
  executeAttackOfTheDead,
  executeSuperSkip,
  executeReverse,
  executeChainStrike,
  executeShieldBash,
  isAttackPackCard,
} from '@arcadeum/games-core/games/critical/critical-attack.utils';
