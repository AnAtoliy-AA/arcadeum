/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  dispatchDeityPackAction,
  executeOmniscience,
  executeMiracle,
  executeSmite,
  executeResurrection,
  executeJudgment,
  executeProphecy,
  executeCommitProphecy,
  executeRapture,
} from '@arcadeum/games-core/games/critical/critical-deity.utils';
