/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export type {
  Point,
  GoPlayer,
  PlaceStonePayload,
  BoardScores,
  GoState,
  InitializeConfig,
} from '@arcadeum/games-core/games/go/go.types';
