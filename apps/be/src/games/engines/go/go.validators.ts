/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  validatePlaceStone,
  validatePassTurn,
  validateForfeit,
  isKnownAction,
} from '@arcadeum/games-core/games/go/go.validators';
