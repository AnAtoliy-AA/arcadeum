/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export type {
  TrickPlay,
  CurrentTrick,
  HeartsPlayer,
  HeartsState,
  PassCardsPayload,
  PlayCardPayload,
  InitializeConfig,
} from '@arcadeum/games-core/games/hearts/hearts.types';
