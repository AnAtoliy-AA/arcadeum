/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export type {
  PachisiToken,
  PachisiPlayer,
  MoveTokenPayload,
  PachisiState,
  InitializeConfig,
  LegalMove,
} from '@arcadeum/games-core/games/pachisi/pachisi.types';
