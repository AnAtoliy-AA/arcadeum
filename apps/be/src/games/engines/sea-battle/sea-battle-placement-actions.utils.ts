/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  runPlaceShip,
  runMoveShip,
  runAutoPlace,
  runConfirmPlacement,
  runResetPlacement,
  runBatchPlacement,
} from '@arcadeum/games-core/games/sea-battle/sea-battle-placement-actions.utils';
