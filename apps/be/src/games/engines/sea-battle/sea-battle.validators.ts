/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export {
  validatePlaceShip,
  validateMoveShip,
  validateConfirmPlacement,
  validateAutoPlace,
  validateResetPlacement,
  validateAttack,
  validateUseSonar,
  validateUseRadar,
} from '@arcadeum/games-core/games/sea-battle/sea-battle.validators';
