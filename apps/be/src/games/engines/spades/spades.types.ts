/** Moved to @arcadeum/games-core (ARC-900 offline mode). Re-export shim. */
export type {
  TrickPlay,
  CurrentTrick,
  SpadesPlayer,
  HandSummary,
  SpadesState,
  BidPayload,
  PlayCardPayload,
  InitializeConfig,
} from '@arcadeum/games-core/games/spades/spades.types';
