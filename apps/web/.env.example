# Backend API base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# WebSocket base URL (optional, defaults to API base URL)
NEXT_PUBLIC_WS_BASE_URL=

# Socket Encryption (AES-256-GCM)
# Set to 'true' to enable socket message encryption
NEXT_PUBLIC_SOCKET_ENCRYPTION_ENABLED=false
# 32-byte hex-encoded key for AES-256-GCM (generate with: openssl rand -hex 32)
# Must match SOCKET_ENCRYPTION_KEY in backend and EXPO_PUBLIC_SOCKET_ENCRYPTION_KEY in mobile
NEXT_PUBLIC_SOCKET_ENCRYPTION_KEY=
