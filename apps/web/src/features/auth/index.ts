/**
 * Auth feature public API
 * Following FSD architecture - only export what's needed by other layers
 */

// Main page component
export { AuthPageContent } from "./ui/AuthPageContent";

// Types
export type { SessionDetailItem, SessionDetailLabels, ProviderCopy } from "./types";
