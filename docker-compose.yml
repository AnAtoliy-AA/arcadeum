services:
  mongo:
    image: mongo:7
    container_name: arcadeum-mongo
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    ports:
      - '127.0.0.1:27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    healthcheck:
      test: ['CMD', 'mongosh', '--eval', "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  be:
    build:
      context: .
      dockerfile: apps/be/Dockerfile
    container_name: arcadeum-be
    restart: unless-stopped
    env_file:
      - ./apps/be/.env
    ports:
      - '127.0.0.1:4000:4000'
    depends_on:
      mongo:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 1.5g
          cpus: '2'
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:4000/ready']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  worker:
    build:
      context: .
      dockerfile: apps/be/Dockerfile.worker
    container_name: arcadeum-worker
    restart: unless-stopped
    env_file:
      - ./apps/be/.env
    deploy:
      resources:
        limits:
          memory: 1g
          cpus: '1'
    depends_on:
      mongo:
        condition: service_healthy

  tg-bot:
    build:
      context: .
      dockerfile: apps/tg-bot/Dockerfile
    container_name: arcadeum-tg-bot
    restart: unless-stopped
    env_file:
      - ./apps/tg-bot/.env
    ports:
      - '127.0.0.1:4001:4001'
    depends_on:
      mongo:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 1g
          cpus: '1'
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:4001/health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  mongo-data:
