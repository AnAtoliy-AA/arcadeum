{
  "name": "aicoapp-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "format": "npm run format:be && npm run format:fe",
    "format:be": "npm --prefix be run format",
    "format:fe": "npm --prefix fe run format",
    "build": "npm run build:be && npm run build:fe",
    "build:be": "npm --prefix be run build",
    "build:fe": "npm --prefix fe run build --if-present",
    "dev": "concurrently \"npm --prefix be run dev\" \"npm --prefix fe run dev\"",
    "test": "npm run test:be && npm run test:fe",
    "test:be": "npm --prefix be run test",
    "test:fe": "npm --prefix fe run test --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.10"
  }
}
